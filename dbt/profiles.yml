nyc_taxi:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID', 'nyc-taxi-491513') }}"
      dataset: "{{ env_var('BQ_DATASET', 'nyc_taxi') }}"
      location: US
      threads: 4
